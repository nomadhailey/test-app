import React from 'react'
import { Container, Label, StyledInput } from './InputStyles'

const Input = () => {
  return (
    <Container>
      {/* <Label htmlFor="email">이메일</Label>
      <Input
        name="email"
        id="email"
        type="email"
        value={email}
        onChange={handleInput}
        placeholder="abc@wncoms.com"
        required
      /> */}
    </Container>
  )
}

export default Input
